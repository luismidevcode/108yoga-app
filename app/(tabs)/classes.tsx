import { View, Text } from 'react-native';

export default function ClasesScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Pantalla de clases</Text>
    </View>
  );
}